# Patterns

Patterns are small reasoning contracts used after local ingestion. They do not parse files, run compilers, or authorize commands.

Each pattern defines an input contract, a security boundary, a method, and an output contract. Load only the pattern needed for the task.
